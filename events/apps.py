from django.apps import AppConfig


class EventsConfig(AppConfig):
    """Configuration for the ZIEGERS DeCipher Gaming (events) application."""
    name = 'events'
    verbose_name = 'Gaming Events'
